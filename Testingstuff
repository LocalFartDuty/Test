--// Key System Setup
getgenv().ValidKey = "Localmadethisnigga" -- Set your unique key here
getgenv().InputKey = "" -- This will store the input key by the user

--// Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local GuiService = game:GetService("GuiService")
local UserInputService = game:GetService("UserInputService")
local TextService = game:GetService("TextService")

--// Variables
local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera
local AimlockState = true
local Victim = nil
local FOVCircle = Drawing.new("Circle")
local HoldingKey = false
local CurrentMousePos = Vector2.new(UserInputService:GetMouseLocation().X, UserInputService:GetMouseLocation().Y)
local IsAuthenticated = false -- Flag to check if the key is correct

--// Create the TextLabel for displaying "#localmadethis" on the top left
local ScreenGui = Instance.new("ScreenGui")
local TextLabel = Instance.new("TextLabel")

ScreenGui.Parent = game.CoreGui
ScreenGui.Name = "TopLeftText"

TextLabel.Parent = ScreenGui
TextLabel.Size = UDim2.new(0, 200, 0, 50)  -- Set size of the label
TextLabel.Position = UDim2.new(0, 10, 0, 10)  -- Position it in the top-left corner
TextLabel.Text = "#localmadethis"  -- The text to display
TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)  -- White text color
TextLabel.BackgroundTransparency = 1  -- Make background transparent
TextLabel.TextSize = 24  -- Set text size
TextLabel.Font = Enum.Font.SourceSans  -- Set the font type

--// Functions

-- Function to ask user for key input
function AskForKey()
    -- Create a GUI to ask for the key
    local ScreenGui = Instance.new("ScreenGui")
    local InputBox = Instance.new("TextBox")
    local SubmitButton = Instance.new("TextButton")

    ScreenGui.Parent = game.CoreGui
    ScreenGui.Name = "KeyInputScreen"
    
    InputBox.Parent = ScreenGui
    InputBox.Size = UDim2.new(0, 300, 0, 50)
    InputBox.Position = UDim2.new(0.5, -150, 0.5, -25)
    InputBox.PlaceholderText = "Enter your key"
    InputBox.Text = ""
    InputBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    InputBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)

    SubmitButton.Parent = ScreenGui
    SubmitButton.Size = UDim2.new(0, 100, 0, 50)
    SubmitButton.Position = UDim2.new(0.5, -50, 0.5, 30)
    SubmitButton.Text = "Submit"
    SubmitButton.BackgroundColor3 = Color3.fromRGB(100, 100, 100)

    -- Wait for user to submit the key
    SubmitButton.MouseButton1Click:Connect(function()
        getgenv().InputKey = InputBox.Text
        if getgenv().InputKey == getgenv().ValidKey then
            IsAuthenticated = true
            ScreenGui:Destroy() -- Remove the key input screen
        else
            -- Show a message if the key is incorrect
            local IncorrectLabel = Instance.new("TextLabel")
            IncorrectLabel.Parent = ScreenGui
            IncorrectLabel.Size = UDim2.new(0, 300, 0, 50)
            IncorrectLabel.Position = UDim2.new(0.5, -150, 0.5, 100)
            IncorrectLabel.Text = "Invalid Key!"
            IncorrectLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
            IncorrectLabel.BackgroundTransparency = 1
        end
    end)
end

--// Ensure key input is received before proceeding
AskForKey()

-- Main script won't continue until user is authenticated
while not IsAuthenticated do
    wait(0.5) -- Check every 0.5 seconds for authentication
end

-- Now that we are authenticated, we can continue with the rest of the script.

-- Setup for the Player, Camera, and UserInput
getgenv().Prediction = 0.15038
getgenv().AimPart = "HumanoidRootPart" -- Default Aim Part
getgenv().Key = Enum.KeyCode.Z -- Key to hold for lock-on
getgenv().DisableKey = Enum.KeyCode.P -- Key to enable/disable aimlock
getgenv().AutoPrediction = true

getgenv().FOV = true
getgenv().ShowFOV = false
getgenv().FOVSize = 100
getgenv().Smoothness = 0.35 -- The smoothness value for gradual target locking

getgenv().AutoJumpOffset = true -- Whether to automatically calculate jump offset

--// Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local GuiService = game:GetService("GuiService")
local UserInputService = game:GetService("UserInputService")

--// Variables
local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera
local AimlockState = true
local Victim = nil
local FOVCircle = Drawing.new("Circle")
local HoldingKey = false
local CurrentMousePos = Vector2.new(UserInputService:GetMouseLocation().X, UserInputService:GetMouseLocation().Y)

--// FOV Circle Setup
FOVCircle.Thickness = 2
FOVCircle.Filled = false
FOVCircle.Color = Color3.fromRGB(255, 255, 0)
FOVCircle.Transparency = 1
FOVCircle.Visible = getgenv().ShowFOV
FOVCircle.Radius = getgenv().FOVSize

--// Functions

function UpdateFOV()
    FOVCircle.Visible = getgenv().ShowFOV
    local mouseLocation = UserInputService:GetMouseLocation()
    FOVCircle.Position = Vector2.new(mouseLocation.X, mouseLocation.Y + GuiService:GetGuiInset().Y)
end

function IsVisible(part)
    local origin = Camera.CFrame.Position
    local direction = (part.Position - origin).Unit * (part.Position - origin).Magnitude
    local ray = Ray.new(origin, direction)
    local hitPart, _ = Workspace:FindPartOnRayWithIgnoreList(ray, {LocalPlayer.Character})
    return hitPart and hitPart:IsDescendantOf(part.Parent)
end

--// Fix FOV to not include players behind you
function IsInFOV(player)
    local screenPos = Camera:WorldToViewportPoint(player.Character[getgenv().AimPart].Position)
    local distance = (Vector2.new(screenPos.X, screenPos.Y) - UserInputService:GetMouseLocation()).Magnitude
    return distance < getgenv().FOVSize and screenPos.Z > 0
end

function GetClosest()
    local closestPlayer = nil
    local shortestDistance = math.huge

    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
            local targetPart = player.Character:FindFirstChild(getgenv().AimPart)
            if targetPart and IsVisible(targetPart) and IsInFOV(player) then
                local screenPos = Camera:WorldToViewportPoint(targetPart.Position)
                local distance = (Vector2.new(screenPos.X, screenPos.Y) - UserInputService:GetMouseLocation()).Magnitude
                if getgenv().FOV and distance < getgenv().FOVSize then
                    if distance < shortestDistance then
                        closestPlayer = player
                        shortestDistance = distance
                    end
                elseif not getgenv().FOV and distance < shortestDistance then
                    closestPlayer = player
                    shortestDistance = distance
                end
            end
        end
    end
    return closestPlayer
end
